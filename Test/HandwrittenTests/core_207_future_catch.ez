// Test that await correctly unwraps and propagates errors
async task throwTask() {
    throw "Task failed"
}

async task catcher() {
    try {
        await throwTask()
        give "no error thrown"
    } catch e {
        give e.message
    }
}

result = await catcher()
assert(result == "Task failed", "Actual error: " + result)
