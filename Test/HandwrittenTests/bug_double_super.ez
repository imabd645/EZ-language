model Parent {
    init() { out "Parent init" }
}
model Child extends Parent {
    init() {
        super()
        try {
            super() // This should throw an error!
        }
        catch e {
            out "this should throw an error"
        } // This should throw an error!
    }
}
Child()
out "Finished"
