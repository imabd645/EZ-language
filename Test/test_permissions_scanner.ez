# Test script for static permission analysis (ez permissions)

# 1. File read
data = readFile("config.json")

# 2. File write
writeFile("output.log", "some result")

# 3. Network request
resp = http_get("https://api.github.com/repos/imabd645/EZ-language")

# 4. Process execution
system("whoami")

# 5. Native FFI
k32 = os_load_lib("Kernel32.dll")

out "Static permissions test script executed."
