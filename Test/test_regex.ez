# ============================================================================
# EZ Regex Engine Test
# ============================================================================

use "lib/unittest.ez"

describe("Regex Engine (Raw Strings)", || {

    it("should match simple patterns with r-strings", || {
        assert(reMatch("hello", r"h.*o"), "Match failed")
        assert(reMatch("12345", r"\d+"), "Digit match failed")
        assert(not reMatch("abc", r"\d+"), "Invalid digit match")
    })

    it("should search and capture groups with r-strings", || {
        matches = reSearch("User: Abdullah, ID: 12345", r"User: (\w+), ID: (\d+)")
        assertEquals(len(matches), 3, "Should have 3 groups (total + 2 sub)")
        assertEquals(matches[1], "Abdullah", "First group mismatch")
        assertEquals(matches[2], "12345", "Second group mismatch")
    })

    it("should replace content using patterns with r-strings", || {
        text = "The quick brown fox jumps over the lazy dog"
        result = reReplace(text, r"[aeiou]", "*")
        assertEquals(result, "Th* q**ck br*wn f*x j*mps *v*r th* l*zy d*g", "Replacement failed")
    })

    it("should handle invalid regex patterns gracefully", || {
        try {
            reMatch("test", r"[")
        } catch e {
            out "  [INFO] Caught expected regex error: " + str(e)
            assert(startsWith(str(e), "Regex Error"), "Should report Regex Error prefix")
        }
    })

})

testSummary()
