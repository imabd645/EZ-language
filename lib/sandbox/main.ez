# ============================================================================
# sandbox -- Secure, Isolated Execution Sandbox for EZ
# ============================================================================

use "src/guard.ez"
use "src/sandbox.ez"

export task create(options = {}) {
    give Sandbox(options)
}

export task eval(code, options = {}) {
    box = Sandbox(options)
    give box.eval(code)
}
