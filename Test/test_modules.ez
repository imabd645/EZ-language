use "Test/lib_a" as a
use "Test/lib_b" as b

resA = a.foo()
resB = b.foo()

out "A: " + resA
out "B: " + resB

if (resA == "A" and resB == "B") {
    out "SUCCESS: Modules are namespaced!"
} else {
    out "FAILURE: Module collision or error."
    exit(1)
}
