try {
    f = spawn(|| {
        wait(500)
    })
    cancel(f)
    await(f)
} catch (e) {
    out "CAUGHT EXCEPTION!"
    out "Message: " + e.message
    out "Stack Trace:"
    get st in e.stackTrace {
        out "  at " + st
    }
}
